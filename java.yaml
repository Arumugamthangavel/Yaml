server:
  port: 8080

spring:
  application:
    name: employee-service

  datasource:
    url: jdbc:mysql://localhost:3306/employees
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
